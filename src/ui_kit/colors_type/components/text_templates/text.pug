<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="./components/text_templates/text.scss")
        title Text
    body
        .text_template
            ul(class="column")
                li(class="li1") H1
                li(class="li2") H2
                li(class="li3") H3
                li(class="li4") Body
            
            p(class="p1") This one is the sub-section or widget title
            p(class="p2") Next one is the item title inside widgets
            p(class="p3") This is a label or CTA text
            p(class="p4") This is the body text which is used for most of the design, like paragraphs, lists, etc.